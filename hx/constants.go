package hx

// HTTP actions
const (
	HXPOST     = "hx-post"
	HXGET      = "hx-get"
	HXRedirect = "hx-redirect"
)

// UI validation
const (
	HXMax                 = "hx-max"
	HXMin                 = "hx-min"
	HXValidationDisable   = "hx-vdisable"
	HXValidationPasswords = "hx-pdisable"
)

// DOM with backend
const (
	HXRequire = "hx-require"
	HXSwap    = "hx-swap"
	HXSend    = "hx-send"
	HXUpload  = "hx-upload"
)

// DOM
const (
	HXDirectEnable  = "hx-enable"
	HXDirectDisable = "hx-disable"

	HXOnChangeEnable = "hx-onchange-enable"

	HXShow       = "hx-show"
	HXShowOnLoad = "hx-show-onload"
	HXHide       = "hx-hide"
)
